---
# Network configuration for the Kubernetes cluster
pod_network_cidr: "192.168.0.0/20"

# This acts as the cluster API control plane registration endpoint
control_plane_endpoint: "k8s-api.internal:6443"